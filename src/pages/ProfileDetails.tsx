import React from 'react';
import { IonButton, IonContent } from '@ionic/react';


function ProfileDetails({ nav }: { nav: any }) {
  return (
    <IonContent class="ion-padding">
      <h1>Profile Details</h1>
    </IonContent>
  );
}

export default ProfileDetails;